# Ren P1-R1 恢复代码发布审查

**时间**：2026-09-04 22:04:22 +08:00

**ARIS workflow**：`experiment-bridge`

**最终裁决**：`BLOCKING / DO_NOT_RUN_R1B_OR_R1C`

**审查性质**：fresh GPT-5.6-Sol xhigh，同模型族 provisional；不宣称跨模型独立性
**用户批准范围**：`APPROVE_REN_P1R1:a7a8f5521b6b249af59a9ded0971cb02f912d9f46e8babfe3d60777cbfcc3c6d`

## 执行边界

- 已发生：固定 RARLAB URL 的本地下载/版本探测；只读 official listing；与旧 7-Zip ledger 的 exploratory 零差异检查。
- 未发生：reviewed runner 的正式 R1A seal、完整 archive test、解压、XLS 打开/解析、Data Gate、模型、预测、RUL、Ark/API、GPU。
- exploratory listing：237 members = 233 regular `.xls` + 4 directories；listed uncompressed bytes = 15,223,551,488；path/size/packed-size/CRC/compression 与旧 ledger 零差异。该结果未形成 release seal，不能解锁 R1B。

## 首轮审查与修复

首轮裁决为 `BLOCKING`，发现：R1A/B/C 未做加密绑定、verifier 可相信伪造 ledger、旧 listing/safety fields 未冻结、下载 receipt 脱离交易、路径未固定到 ignored tree、失败未可靠隔离、强停止测试不足、CSV 缺少禁止执行标志。

一次决定性修复加入了：

- 固定项目内 run path 与 Git ignore 预检；
- 集成 HTTPS `curl` receipt、下载页 7.23 证据和安全 tar 展开；
- hash-pinned prior ledger 与 link/encryption/redirection 拒绝；
- R1A/R1B/R1C 非循环 byte seals；
- full test/extraction 原始 transcript、逐 member size/CRC/type ledger；
- verifier 自行重跑 official listing、对照旧 ledger、解析 test transcript，并第二遍扫描提取成员 CRC；
- 13 项定向 helper/failure tests。

审查所见代码哈希：

| File | SHA-256 |
|---|---|
| `experiments/audit_cap/ren_p1r1_recovery.py` | `d0f8d27214b55095d3f18adaf5ecbbcb143d8c2d6d11d9ba9f74d52f24d4b021` |
| `experiments/audit_cap/verify_ren_p1r1_recovery.py` | `d6538539c3b9563c1659fca52a65c64e6c90260b166c35cbb59e5bb048af1ec3` |
| `tests/test_ren_p1r1_recovery.py` | `f7b72ec89e78aa70a0b17f8c7b6bf7b3d0239d59d7be8bea8e4044e683dcf151` |
| `tests/test_verify_ren_p1r1_recovery.py` | `726e627184ac953d6eee61a2378ee951ada0ec1cfe3229dcbbedef4c1ce8af74` |
| `.gitignore` | `4ce26fe12a0741b212583356c60d508572e8ccc512cbd417cd90ab2e34434fa4` |

## 唯一复审的剩余阻断

复审仍为 `BLOCKING`：

1. raw headers/HTML/listing/test/extraction/version transcripts 被设计到 tracked audit tree；冻结计划要求它们 local-only，且 header 可能泄漏 redirect query/credential。
2. 13 项测试仍未覆盖 `Paths.build`、download、tar extraction、R1A/B/C、完整 verifier 的 mocked end-to-end 强停止分支。
3. phase seals 没有绑定 generator、verifier、相关 tests 和 `.gitignore` 的 bytes。
4. JSON/tar/Unicode/filesystem 等非 `RecoveryError` 异常仍可能绕过 `*_BLOCKED.json`。
5. verifier 未逐字段重建 transport、test report 和 extraction transcript/manifest 的全部结构化字段。
6. verifier 未再次检查 ignored containment 与全部祖先 symlink。

## 强停止裁决

ARIS `experiment-bridge` 只允许一次修复后复审；第二次仍为 `BLOCKING`，所以本 generation 必须停止。当前代码只能作为 blocked remediation draft，不是可运行 release。不得运行 R1B/R1C，不得以 listing 零差异声称 archive recovery PASS。

下一 generation 必须先把 raw logs 移至 ignored staging、把代码/测试/ignore policy 加入 seals、统一捕获并记录 anticipated failures、扩展 verifier 的逐字段重建、加入 mocked end-to-end adversarial suite，然后重新进行 fresh pre-run review。

测试证据：Ren 定向 suite `13 passed`；冻结 N0+ 环境下全项目 `379 passed`。在 audit-only 环境运行全项目时另有 2 个 N0+ tests 因该环境不含 `statsforecast` 而失败，属于环境不匹配，不作为回归 PASS；可比的冻结 N0+ 环境全通过。

完整两轮 prompt/response 保存在本地 `.aris/traces/experiment-bridge/2026-09-04_run01/003-*` 与 `004-*`，不提交 Git；`.aris/meta/events.jsonl` 仅记录 provenance 元数据。
