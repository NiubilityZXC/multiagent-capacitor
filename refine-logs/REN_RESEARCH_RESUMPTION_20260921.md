# Ren 科研恢复与实验启动估计

日期：2026-09-21。沿用既有 P1-R1 / R2 批准；不新增真实审计重跑、P2 或 API 授权。

## 已核实状态

生产 R2 全量审计已经退出 0；覆盖 113 来源命名组、104,190,778 行。它的完成不等于 Data Gate 通过。历史失败运行与所有 seals 保留。

独立 post-run 审查在上轮额度中断后恢复。其首个诊断 job 的 RETURNED.json 实际为 child_returncode=1、elapsed_seconds=0.6008604711387306。审查者报告：其脚本错误地比较了外部 OLE 文件哈希与内部 Workbook 流哈希，导致 metadata 阶段退出；SQLite、候选/匹配和全量指纹阶段尚未执行。不能把计划中的重建说成完成。

原失败诊断保存在 ignored `data/raw/ren_scs/r2_postrun_review_20260920/`。同一独立审查者正在保留首轮现场并准备单独的修复诊断；不重新扫描原始 XLS，不改写生产结果。审查路线为 same-family / provisional，尚无最终 verdict。

容量工步纯函数与合成测试已完成独立工程复审；真实容量资格审计的完整 caller、汇总表对照和实际验证尚未完成。

## 距离实验的三个关口

1. 独立重建对账完成，并判断跨组匹配对设备身份与切分的影响。
2. 完成容量可推导性、设备/工况映射、终止/删失和 whole-unit 切分的 Data Gate；ESR / RUL 资格与容量预测分别裁决。
3. 冻结精确 P2 数据、target、eval 与代码，完成对应复审/放行，然后启动数值基线。

粗略排期：在没有新增数据冲突、算力/额度中断的条件下，数值基线目标为再需 1–2 天有效工作时间；这是工作量估计而非保证开始日期。当前不能承诺今天开跑。LLM Agent 还需安全凭据、能力探测及预算范围确认；不会使用对话中暴露的旧密钥。

本记录不表示 Data Gate PASS、模型结果或论文性能结论。上轮 MANIFEST 补记已经完成，本轮随文档同步 GitHub；原 09-20 tracker 中关于 MANIFEST 待补的句子是历史状态。
