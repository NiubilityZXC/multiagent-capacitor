# Ren P1-R1 R2 修复与测试快照

状态：`PRE_RUN_REVIEW_PENDING / NO_REAL_ARCHIVE_TEST_OR_EXTRACTION`。

沿用 2026-09-04 固定批准记录与计划。R2 自 2026-09-05 开始修复，经额度/会话中断，于 2026-09-07 接续；没有重置原 generation 或删除上轮 BLOCKING 记录。

- 原始 download headers/HTML/curl、版本、listing、df、archive test、extraction transcripts 全部改入 ignored staging。新 ARIS review traces 也 ignored，已有 tracked 历史保留。
- seals 绑定 generator、独立 verifier、两份相关测试、`.gitignore`、固定批准与未来 pre-run release；release 尚未生成。
- verifier 从原始证据逐字段重建 transport receipts、tool identity/hash ledger、listing diff、R1A preflight、test report、extraction manifest，另行重跑 listing 和重新计算提取文件 CRC。
- 真实 mock E2E 链发现 `_extract_tool` 对已存在 `rar/` parent 重建失败，已修复。每份结构化报告的每个顶层字段均经过修改并重新计算 seals，完整 verifier 仍须拒绝。
- R1A tool/page/redirect/version/disk/listing failure、R1B warning/timeout/exit/stderr/error、R1C wrong path/CRC/link/warning/timeout/exit/stderr/error 均验证强停止。
- `-idp` 依据固定官方包 `rar.txt` 1218 行，只关闭进度百分比，保留逐成员 OK 与 All OK。

测试：恢复定向 `62 passed`；本项目明确限定 `tests/` 的全量回归 `479 passed`（40.87 s）。根目录无范围 pytest 会同时扫描内嵌 ARIS 的同名 tests 包，曾产生收集错误；规范回归命令使用 `python -m pytest -q tests`。后续静态 caller 又有 3 项合成测试通过，尚未计入上述 479。

另有 R1D 静态组件及 caller 草稿，合计 54 项合成测试通过；不在本次 R1ABC release 复审范围，真实部署前另审。独立解析环境已冻结并安装，仅做 pip check/import smoke，未打开实际 workbook。

本快照仅保存实现进展，不能替代 pre-run PASS，不授权 R1B/R1C 或模型执行。完整原始复审请求位于 ignored `data/raw/ren_scs/review_r2_20260906/`；正式裁决另行记录。
