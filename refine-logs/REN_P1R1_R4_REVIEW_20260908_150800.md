# Ren P1-R1 R4/v5 运行前复审

状态：`PASS_TO_RUN_R1ABC`；沿用原批准，不扩大范围。

本轮仅修复 R3 已捕获的 UnRAR 绝对路径输出与相对路径预期不一致。生成器和独立 verifier 分别以隔离目录与成员路径建立精确绝对路径列表；不使用 basename 匹配或宽松归一化。R2/R3 失败运行保持原样、不得解锁。

ARIS `experiment-bridge` 要求的 fresh GPT-5.6-Sol xhigh 审查已完成：0 blocking、0 non-blocking。这是 same-family provisional 工程复审，不是科学有效性验证。审查者执行 72 项合成测试，通过（8.51 秒）；主执行者完成全项目 tests/ 回归，492 项通过（51.15 秒）。

完整请求/响应保存在 ignored staging `data/raw/ren_scs/review_r4_20260908/`，响应 SHA-256：`85a23bef6c78ef0143c8938d42f3d92c43e994edd21d82dd6d1a43ff43b12ca2`。完整 trace：`.aris/traces/experiment-bridge/2026-09-08_run01/005-ren-r4-prerun-review-pass`。五个已核对 policy hashes 见 `REN_P1R1_R4_RELEASE_20260908_150800.json`。

R1D 的 R4 兼容性补充响应已在先前 quota 错误发生前写入 staging；本轮读取并保留完整内容，而非把失败调用当作新审查。六项静态 policy 文件字节未变，原 R1D PASS 在新 v5 恢复与独立验证通过后继续有效。补充响应 SHA-256：`ef938fc40c934e21df5e767c70eaf65c2a3a879436bc70e506a46edce2b04d75`；恢复 provenance 见 trace `004-ren-r1d-r4-compatibility-recovered`。

新运行 ID：`p1r1_20260908_150800`。本记录只证明运行前放行，不声明真实运行成功。R1D 若出现无法分类的结构，按冻结计划停止行解析。R1F Data Gate 与新 P2 人工批准前，不执行模型、RUL、Ark API、GPU 或性能主张。
