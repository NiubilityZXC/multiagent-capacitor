VERDICT BLOCKED_PROTOCOL_AMBIGUITY

BLOCKERS

- `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:188-402` — The named schemas use `#/$defs/...`, but each schema has its own `$id` and no local `$defs`; extracting a named schema leaves unresolved references, while the top-level registry is not itself a JSON Schema. Exact correction: publish one valid Draft 2020-12 schema resource with shared `$defs` and named schemas beneath it, or make every `$id` resource self-contained; hash-pin that resource and its validation executable/tests.
- `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:770-862`, `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:1027-1074` — `FinalDecision.v1` requires an unconstrained numeric/action `payload`, although all w4 prompts say the local executor performs selection/aggregation and workers may not invent values. The schema neither binds `decision_code` to `aggregation_id`/payload type nor proves payload equality to a parent/local deterministic result. Exact correction: make provider output a candidate-specific, discriminated reference-only decision; create a separately hashed local `FinalOutput.v1`; enforce each decision-code/parent/aggregation mapping with closed schema branches or a hash-pinned validator/executor.
- `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:541-593`, `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:947-1005`, `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:1061-1069`, `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:1788-1833` — Each A03 specialist emits two artifacts, but slot closure has only one output-artifact hash and the w4 parent contract does not define whether it hashes the response array, either child, or a wrapper. Exact correction: define and hash-pin an `A03SpecialistOutput.v1` envelope containing both child artifacts/hashes; point `SlotClosure.output_artifact_hash` to that envelope and require w4 parents to be exactly route-envelope, w2-envelope, and w3-envelope.
- `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:674-768`, `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:1281-1393` — `RoutePlan.v1` permits provider selection of `R00_DEFAULT_N0_GLOBAL` despite `router_selectable=false`, arbitrary route/assignment mismatches, duplicate specialist roles, and view-role mismatches. Exact correction: encode each route as a closed `oneOf` branch with exact assignments, role, and `route_origin`, reserving R00 exclusively for `local_route_default`; likewise enforce specialist actions against the assigned view through a hash-pinned validator.
- `refine-logs/PLAN_A_ARCHITECTURE_CANDIDATES.md:142-150` — Fold-local `M_X1..M_X4` assignment ranks an undefined “training-only role loss,” particularly for router, critic, and arbiter roles, so roster selection is not mechanically reproducible. Exact correction: define each role/model score and data split, or enumerate all admissible injective four-model mappings and select by full-candidate nested whole-unit loss/reliability gates with a fixed lexicographic tie rule; include all evaluation slots in `N_dev`.
- `refine-logs/PLAN_A_ARCHITECTURE_REGISTRY.json:1541-1554` — A response “after durable timeout or fallback closure” transitions from `STARTED` to terminal `LATE_DISCARDED`, although durable timeout already created a terminal state; this conflicts with immutable one-terminal-closure semantics. Exact correction: retain the original terminal failure state and record late discard as an orthogonal immutable event/status that cannot replace slot closure.
- `refine-logs/PLAN_A_P2_FREEZE_PROPOSAL.md:121`, `refine-logs/PLAN_A_P2_FREEZE_PROPOSAL.md:140-166`, `refine-logs/PLAN_A_HYPOTHESIS_REGISTRY.json:576-593` — Prose makes Bonferroni simultaneous component bounds part of Plan A gates, but machine `tier_mapping` requires only Holm composite rejections. These can disagree, leaving the claim tier non-mechanical. Exact correction: add the exact required component-bound crossings to `tier_mapping`—point NI/superiority, operational, harm, and absolute reliability bounds—in addition to Holm rejections, or designate one criterion consistently across all files.

NONBLOCKING

- Recomputed canonical hashes match the controlling directive.
- Every file and P1 bundle root listed in `PLAN_A_PRESEAL_BUNDLE_MANIFEST.json` matches; both P1 internal hash lists validate completely.
- All 12 prompt hashes and the generation-ID derivation validate.
- Visible-prefix one-step MASE, global `BLOCKED_ARCH_DEV`, post-tournament/pre-seal C4 selection, separate `resilience_v1`, P6-before-Plan-B, pending signed baseline decision, conservative N0 wording, current P1 authority, 19-slot execution count, no-monetary-claim rule, canonical `11+ARCH1`, and single seal/barrier/unseal are otherwise consistent.