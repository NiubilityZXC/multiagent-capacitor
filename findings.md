# AUDIT-Cap findings

## Current verdict

- Final experiment audit: overall `WARN`; numerical/hash integrity `PASS`.
- Supported positive claim: deterministic parser/replay/ledger and quick synthetic-design harness operation and same-code reproducibility, within the stated Stress-2 surrogate sanity scope.
- Unsupported: general model superiority, independent-device inference, RUL/interval accuracy, cross-condition robustness, formal Design Gate, champion selection, Benchmark-L readiness, and Agent-topology advantage.

## Claim gaps

- Stress-2 six columns have unverified physical identity and duplicate structure.
- Causal chronology is same-process software self-attestation, not externally anchored proof.
- Large-pack reference linkage, acquisition-time ordering, 20-token-to-18-column mapping, ES12 four-row mismatch, NaN masks, identity, duplicates, capacity/ESR targets, and termination/censoring remain unresolved.
- Quick design uses 200 repeats per cell and is explicitly non-confirmatory; the formal Design Gate was not run.
- Freeze B is not the immediate next stage: protocol order is Data Gate, formal Design Gate, then Freeze B.

## Next experiments

1. Build and freeze only the reference-aware large-pack parser and Data-Gate tests.
2. Resolve or quarantine identity, duplicates, event time, missingness, target semantics, and outcome semantics.
3. Keep all Benchmark-L models and RUL code paths blocked until their gates pass.
4. Parameterize and run the formal design simulation only after the Data Gate.
5. Complete Freeze B after Data and Design Gates; then run frozen nested LOCO/LOVO and shadow validation.

Performance ablations are not warranted yet because no eligible performance claim or benchmark exists.

## 2026-08-24 direction shift: Hybrid Agent forecasting

- Research priority is now LLM API multi-Agent prediction and LLM-guided combinations of deterministic numerical experts.
- A safe main mechanism is selected: LLM agents may route or convexly weight frozen small-model forecasts; production Hybrid points remain deterministic and inside the candidate hull. Direct LLM numerical forecasting is a separate control arm.
- Strong near neighbors already exist: CastFSR, REATS, LLM Forecasting Planner, KairosAgent, and TimeClaw. Generic agent routing/fast-slow-reflect is not a defensible novelty claim.
- The no-network core implements strict causal packets, three roles, four topologies, training-only fallback, ArkCLI dry-run, and secret-safe environment handling. Full regression status is 64 tests passed.
- The AgentPlan Base URL was recognized by client preview as a stateless agent-plan lane for logical model `kimi-k3`; this does not prove account permission, server model version, cost, or accuracy.
- No real API call, token use, Stress-2 Agent prediction, Benchmark-L model, or RUL run occurred.
- Stress-2 remains engineering sanity only. Formal Hybrid accuracy requires target-specific Benchmark-L Data Gate, physical identity/duplicate resolution, formal Design Gate, and Freeze B.
- Before any real API pilot, rotate the key exposed in chat and inject it only through `ARK_API_KEY`; provide the base URL through `ARK_BASE_URL`.

## 2026-08-24 P1 real Data Gate and method refinement

- The approved 5.04 GB reference-aware P1 run completed without models or RUL. Its sealed bundle independently verified `PASS`, but the scientific bundle status is correctly `FAIL`.
- Structural facts are now mechanically reconciled: 1,752 EIS unit-events, 9,316 raw slots, 8,835 nonempty pairs, 481 paired empties, 0 quarantined slots, and no exact EIS-matrix duplicate.
- Blocking findings remain: inferred rather than measured EIS availability, one ES10 timestamp reversal, ES12 77,241-vs-77,237 alignment, unknown physical identity, exact ES12/ES14 transient-time duplicate candidate, no frozen capacity target, no ESR/SOH derivation, and no outcome/RUL semantics.
- Therefore Benchmark-L modeling, direct LLM forecasts, hybrid forecasts, and all RUL remain blocked. A parser `PASS` is not a target/Data Gate `PASS`.
- The original four-role MVES route selector received a 5.40/10 same-family `RETHINK`: finite routes are deterministically enumerable, while expressive route signatures lack independent-unit mature support.
- Round-1 refinement changes the statistical object from a unique route to a frozen `AgentPolicy`: one online LLM synthesizes a typed contextual forecast program per origin, deterministic code compiles/tests it, and the complete policy—including invalid/fallback outcomes—is validated on mature whole-unit inner replay.
- Default four-role and LLM challenge-selector components were removed. Homogeneous and heterogeneous multi-agent direct/program-synthesis systems remain required matched-budget comparator arms.
- If deterministic symbolic search, manual programs, finite routing, or the numerical champion match the program-synthesis policy, the LLM method claim must be deleted and the work reframed as a rigorous empirical/negative architecture study.
- AgentPlan discovery remains blocked pending authenticated model-list and actual-resource snapshots plus minimal strict-schema capability probes. No chat-exposed credential will be reused or stored.
